{
  "name": "full_stack_open_osa3_t1",
  "version": "1.0.0",
  "description": "Osa 3 ensimmäinen tehtävä, puhelinluetton backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../full_stack_open/osa2/t6 && npm run build && cp -r dist ../../../full_stack_open_osa3_t1"
  },
  "author": "Joni Töyrylä",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "rotating-file-stream": "^3.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
